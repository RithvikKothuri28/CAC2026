{
  "name": "farmtwin-functions",
  "private": true,
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "overrides": { "gaxios": { "uuid": "11.1.1" } },
  "scripts": {
    "build": "tsc",
    "test:unit": "npm run build && node --test test/unit.test.cjs",
    "test:emulator": "npm run build && firebase emulators:exec --project demo-farmtwin --config ../firebase.json --only auth,firestore \"node --test --test-concurrency=1 test/emulator.test.cjs\"",
    "test": "npm run test:unit && npm run test:emulator"
  },
  "dependencies": {
    "firebase-admin": "14.4.0",
    "firebase-functions": "7.4.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "@types/node": "22.19.15",
    "firebase": "12.10.0",
    "firebase-tools": "15.30.2",
    "typescript": "5.9.3"
  }
}
